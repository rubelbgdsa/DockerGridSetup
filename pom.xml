public EnvironmentPermissions getPermission() {
    Iterator<EnvParams> iterator = getPermissionEnv().iterator();
    if (iterator.hasNext()) {
        EnvParams params = iterator.next();
        String envName = params.getEnvName().toString();
        return nonProdPermission(envName);
    }
    return null; // Return null if there are no more permissions
}
