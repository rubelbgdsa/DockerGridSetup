import java.util.List;
import java.util.stream.Collectors;

public List<EnvironmentPermissions> getPermissionList() {
    return getPermissionEnv().stream()
            .map(params -> {
                String envName = params.getEnvName().toString().toUpperCase();
                System.out.println("getPermissionList === " + envName);
                return nonProdPermission(envName);
            })
            .collect(Collectors.toList());
}

    
    
List<EnvironmentPermissions> permissionsList = getPermissionList();
for (EnvironmentPermissions permissions : permissionsList) {
    // Handle each EnvironmentPermissions object
}

    
    
List<EnvironmentPermissions> permissionList = planspec.getPermissionList();
Bambooserver.publish(permissionList);
